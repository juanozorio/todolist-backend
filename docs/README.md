# Swagger Docs

This directory is generated automatically by `swag init`.

Run the following command to regenerate:

```bash
swag init -g cmd/api/main.go -o docs
```

The `docs/` directory is git-ignored. It is generated during the Docker build and in CI.
